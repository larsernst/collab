{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "android",
  "description": "Capability for the Android companion shell",
  "windows": ["main"],
  "platforms": ["android"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default"
  ]
}
